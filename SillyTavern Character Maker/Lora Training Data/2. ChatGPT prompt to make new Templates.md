# Master Template Instructions

I am designing structured schemas for LoRA training data that will be converted to JSONL and used with Oobabooga.

Your task is to create or update schema templates and schema instances with high structural rigor and consistency.

────────────────────────  
TOP-LEVEL STRUCTURE RULES  
────────────────────────

Every schema template or schema instance MUST include these top-level fields:

- "schema_type" // identifies the domain (e.g., kink, item, action, character, archetype, scale, situation, etc.)
- "instruction" // describes what the schema should generate
- "input" // natural-language query format; should be an array of strings representing multiple phrasings (can include dynamic placeholders like <object_name> and <narrative_text>)
- "output" // the structured schema definition

Optional, domain-dependent meta-spec fields:

- "_global_schema_notes" // schema-level modeling rules and policies
- "placeholder_conventions" // placeholder rules, only when relevant to the domain
- "version" // schema version identifier
- "schema_notes" // meta-notes about schema design (not about ML or training)

────────────────────────  
INPUT STRUCTURE RULES  
────────────────────────

1. Use **placeholders** in the input array to support dynamic triggering from narrative text.
2. `<placeholders>` (e.g., `<object_name>`) should be replaced with concrete values during instantiation.
3. `[ACTOR]` and `[RECEIVER]` remain symbolic and are **never replaced**.
4. Include multiple phrasings per template to catch narrative variations, including verbs, adjectives, and object references.
5. Input examples may include freeform narrative using `<narrative_text>`.

────────────────────────  
PLACEHOLDER RULES  
────────────────────────

1. Use `placeholder_conventions` to define which placeholders exist and how they are used.
2. `<placeholders>`: replaced with concrete domain values, e.g., `<object_name>` → 'dildo', 'fist', 'stick'.
3. `[symbolic_placeholders]`: remain symbolic, e.g., `[ACTOR]`, `[RECEIVER]`.
4. Each placeholder in `allowed_placeholders` should include a usage guideline explaining context and replacement rules.
5. Do not invent placeholder systems beyond the defined rules unless domain requires it.

────────────────────────  
OUTPUT STRUCTURE RULES  
────────────────────────

1. Every `output` object must begin with the following fields in order:

- "id" // unique identifier
- "name" // primary display name
- "category" // high-level domain categories
- "description" // textual description of the domain entity

2. Optional or extended fields follow (participants, objects, phases, thresholds, threshold_model, extensions, addons, metadata, etc.).
3. **Scoped `notes`**:  
   - Participants: notes about their role/behavior  
   - Objects: notes about object usage and replacement of `<object_name>`  
   - Phases: notes about that phase  
   - Thresholds: notes about the threshold mechanics  
   - Threshold_model: notes about threshold progression logic  
   - Top-level `notes`: notes about the situation/schema as a whole

────────────────────────  
CONTENT RULES  
────────────────────────

4. Templates must define required fields, placeholders, and structural patterns explicitly.
5. Avoid vague fields; use structured fields whenever possible.
6. Include bidirectional progression or residual adaptation when applicable.
7. Ensure placeholders in input and output are consistent with `placeholder_conventions`.
8. Document placeholder usage clearly to ensure correct instantiation.

────────────────────────  
META-SCHEMA PRINCIPLES  
────────────────────────

- Domain data belongs in "output".
- Schema modeling rules belong in "_global_schema_notes".
- Placeholder logic belongs in "placeholder_conventions".
- LoRA, ML, or training context must not appear in the schema fields.

────────────────────────  
FORMATTING RULES  
────────────────────────

1. All JSON must use tab (`\t`) indentation.
2. Empty lines between objects or arrays must contain at least one tab.
3. Maintain consistent tab spacing for nested structures.
4. Comments (`//`) in templates must follow the same tab indentation level as the fields they describe.

────────────────────────  
PLACEHOLDER CONVENTIONS TEMPLATE EXAMPLE  
────────────────────────

```json
"placeholder_conventions": {
	"purpose": "Define which placeholders are used in the situation and how they should be replaced.",
	"allowed_placeholders": [
		"[ACTOR]",
		"[RECEIVER]",
		"<situation_name>",
		"<narrative_text>",
		"<object_name>",
		"<phase_name>",
		"<threshold_name>",
		"<condition>",
		"<state_change_forward>",
		"<state_change_backward>",
		"<resistance_change_forward>",
		"<resistance_change_backward>"
	],
	"usage_guidelines": {
		"general_rule": "Use placeholders consistently. `[ACTOR]` and `[RECEIVER]` remain symbolic. All `<placeholder>` values must be replaced with concrete domain values when instantiating a situation.",
		"narrative_text_rule": "Use `<narrative_text>` when converting freeform narrative into a structured situation.",
		"situation_name_rule": "Use `<situation_name>` as the identifier of the situation type; replace it with the actual scenario name.",
		"object_name_rule": "Use `<object_name>` to dynamically capture the object referenced in the narrative (e.g., dildo, fist, stick)."
	}
}

────────────────────────  
INPUT EXAMPLES TEMPLATE  
────────────────────────

"input": [
	"Convert the following text into a structured <situation_name> scenario involving <object_name> and [RECEIVER]: <narrative_text>",
	"Analyze the actions of [ACTOR] inserting <object_name> into [RECEIVER] from the narrative: <narrative_text>",
	"Structure the sequence where [ACTOR] pushes <object_name> into [RECEIVER]: <narrative_text>",
	"Describe how [ACTOR] lines up and inserts <object_name> into [RECEIVER] in steps: <narrative_text>",
	"Break down the forward and backward movement of <object_name> between [ACTOR] and [RECEIVER]: <narrative_text>"
]


────────────────────────  
OUTPUT EXPECTATIONS  
────────────────────────

- Templates must be explicit, extensible, and logically organized.
    
- Schema instances must be complete, consistent, and domain-accurate.
    
- Core fields (`id`, `name`, `category`, `description`) always come first.
    
- Optional or extended fields follow, and `notes` always comes last.
    
- Structural clarity and semantic precision are prioritized over brevity.
    

────────────────────────  
TASK  
────────────────────────

Proceed with the requested schema type or schema update.

Remember: schemas will eventually be converted to JSONL for LoRA training in Oobabooga. Ensure all domain-specific rules, optional extensions, scenarios, and placeholders are fully explicit and consistent. Do NOT reference LoRA, training pipelines, or Oobabooga inside any schema field.

────────────────────────  
EXAMPLE OUTPUT ORDER  
────────────────────────

```json
{
	"output": {
		"id": "example_kink_voyeurism",
		"name": "Voyeurism",
		"category": ["Kink", "Voyeurism"],
		"description": "Interest or engagement from observing others in private contexts.",
		
		"roles": {
			"giver": {
				"id": "voyeur_giver_role",
				"label": "Giver",
				"definition": "The participant being observed"
			},
			"receiver": {
				"id": "voyeur_receiver_role",
				"label": "Receiver",
				"definition": "The participant observing the other"
			}
		},
		
		"consent_and_safety": {
			"explicit_consent_required": false,
			"risk_level": "low"
		},
		
		"notes": "Always respect privacy and consent."
	}
}
```

────────────────────────  
CANVAS MANAGEMENT RULES  
────────────────────────

- A canvas named "Master Template Instructions" must always exist in this conversation.
    
- When initializing or reconstructing master instructions, always create or update a canvas with the exact name "Master Template Instructions".
    
- The canvas must include a self-identifying header containing CANVAS_ID and CANVAS_NAME.
    
- The following Makefile-style directive must be included for reference:
    

```makefile
MASTER_CANVAS_NAME := Master Template Instructions
MASTER_CANVAS_ID := <real_internal_ID>
```

Await further instructions