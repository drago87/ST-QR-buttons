Remove starting \`\`\`json and ending \`\`\` before pasting it into the Kink template.
```json
			// This block is pasted directly inside the "schema_type": "kink" when the kink has combat mechanics. It replaces the existing combat_extension
			"combat_extension": {
				"scope": "combat", // for group dynamics, etiquette, or roleplay context
				"interaction_structure": {},
				"participants": [],
				"intensity_scale": [],
				"compatibility": {},
				"consent_model": {},
				"notes": ""
			}
```