Remove starting \`\`\`json and ending \`\`\` before pasting it into the Kink template.
```json
			// This block is pasted directly inside the "schema_type": "kink" when the kink has social mechanics. It replaces the existing social_extension
			"social_extension": {
				"scope": "social", // for group dynamics, etiquette, or roleplay context
				"interaction_structure": {},
				"participants": [],
				"intensity_scale": [],
				"compatibility": {},
				"consent_model": {},
				"notes": ""
			},
```