I'm creating LoRA training data for a language model. Below is the template we will use for making a [template_name] schema:

Replace this line with the template. Also replace [template_name] with the actual type of schema being created (e.g., Action, Character, Anatomical Size, Archetype, etc.).

Your tasks:

1. Define new schema instances that follow this template.
2. Update existing schema entries if missing fields, errors, or improvements are identified.
3. Ensure all required fields, placeholder conventions, and example structures are correctly applied.
4. If any instruction or input is unclear or inconsistent, ask for clarification before generating output.
5. Use `// (description)` only in the template itself for explanatory notes. **Do not include `//` comments in any actual schema instances.**
6. id's don't need numbers.
7. Always return the completed schema in a JSON markdown block (or canvas-style block) suitable for direct use in training.
8. **All JSON must use tab (`\t`) indentation.**
9. **Empty lines between objects or arrays must contain at least one tab (`\t`)** to prevent automatic closing or joining of `{}` or `[]` by the program.
10. Maintain consistent tab spacing for nested structures.
11. Comments (`//`) should also follow tab indentation consistent with the level of the object they describe.
12. **These schemas will eventually be converted to JSONL format for LoRA training using Oobabooga.**  
    Do **not** include references to LoRA, ML-ready, or Oobabooga inside any field of the schema (especially `notes`).  
    All fields should remain strictly about the item, kink, or concept being described.

The first schema we will create should correspond to the `schema_type` specified in this template. Be precise, thorough, and consistent in applying the template structure.

Await further instructions.